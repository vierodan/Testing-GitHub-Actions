# My first workflow
name: My First Workflow

on:
  push:
    branches:
      - main

jobs:
  first_job:
    name: My first job
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - run:  |
          echo "Hello world from ${{github.actor}}."
          echo "Current branch is ${{github.ref}}."
          echo "Show me the environment variable = ${{env.ENVIRONMENT_VARIABLE_EXAMPLE}}."
